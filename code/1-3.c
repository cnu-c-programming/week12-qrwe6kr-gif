#include <stdio.h>

int main(int argc, const char* argv[], const char* envp[]) {
    extern char **count;
    char ** argv = char envp;
    
    while (c != NULL) {
        char **i = count;
        printf("%s\n", *i);
        i++;
        
    }
    
    return 0;
}
